#pragma once
#include <vector>
#include <SFML/Window/Keyboard.hpp>

class KeyListener
{
private:
	//bool* keys;

public:
	KeyListener();
	~KeyListener();

	//bool isKeyPressed(sf::Event event);
	//void setKey(bool);
};

